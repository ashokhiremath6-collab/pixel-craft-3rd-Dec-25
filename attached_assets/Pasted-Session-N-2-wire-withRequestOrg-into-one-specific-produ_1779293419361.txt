Session N+2: wire withRequestOrg into one specific production route as the first real use. Verify it behaves correctly with a logged-in user against the live database.

Route to wire: GET /api/auth/user (or whatever the equivalent is — the one that returns the current logged-in user's profile). We pick this route because:
  - It's authenticated (uses isAuthenticated)
  - It already runs on every page load, so we can test by just refreshing the app
  - It does not modify data, so it's safe to wrap even if something is wrong
  - It returns a small, easy-to-inspect response

==============================================================
STEP 1 — Pre-flight (read-only)
==============================================================

1.1  git status (should show server/tenantContext.ts as untracked, nothing else modified)

1.2  Confirm the two middleware files still exist and are unchanged from previous sessions:
       ls -la server/tenantAuth.ts server/tenantContext.ts
       wc -l server/tenantAuth.ts server/tenantContext.ts

1.3  Find the route that returns the current logged-in user:
       grep -n "app\.get.*\b/api/auth/user\b\|app\.get.*\b/api/user\b\|app\.get.*\b/api/me\b" server/routes.ts server/localAuth.ts
     If none of those match, search broader:
       grep -nE "app\.(get|post)\(.*['\"](\/api\/auth|\/api\/user|\/api\/me|\/api\/whoami)" server/routes.ts server/localAuth.ts
     Show me what you find. We want one route, currently used by the frontend on app load, that takes isAuthenticated and returns user data.

1.4  For the route you found, show me the full handler (the line with app.get plus the next 15 lines) so I can see its current middleware chain and what it returns.

1.5  Confirm the route is actually called by the frontend on app load:
       grep -rn "auth/user\|api/user\|api/me\|api/whoami" client/src --include="*.ts" --include="*.tsx" | head -10

1.6  npm run check 2>&1 | grep -c "error TS"
     Record baseline.

STOP HERE. Show me Step 1's output. Wait for "proceed to Step 2" before continuing. I need to confirm which route we're wrapping before any code changes.

==============================================================
STEP 2 — Wire withRequestOrg into the chosen route (DO NOT START UNTIL TOLD)
==============================================================

This step assumes I have confirmed which route to wrap. Do not guess.

2.1  Add to server/routes.ts imports (if not already present):
       import { withRequestOrg } from "./tenantContext";

2.2  Modify the chosen route's middleware chain. The existing chain probably looks like:
       app.get("/api/auth/user", isAuthenticated, async (req, res) => { ... });

     Change it to:
       app.get("/api/auth/user", isAuthenticated, withRequestOrg, async (req, res) => { ... });

     CRITICAL: withRequestOrg goes AFTER isAuthenticated (so req.user exists) and BEFORE the handler. Do not change anything else about the handler body.

2.3  Add temporary diagnostic logging INSIDE the handler body to confirm the middleware ran correctly. We want to see, on every call, whether the request was wrapped in a transaction with the org variable set. Add this as the FIRST line of the handler body:

       // TEMP: tenant-context diagnostic. REMOVE at end of session.
       try {
         const { requestDb } = await import("./db");
         const { sql } = await import("drizzle-orm");
         const { db } = await import("./db");
         const conn = requestDb();
         const result = await conn.execute(sql`SELECT current_setting('app.current_org_id', true) AS org_id`);
         const rows = (result as any).rows ?? result;
         console.log("[tenant-context-diagnostic]", {
           route: req.path,
           userId: (req.user as any)?.id,
           userOrgId: (req.user as any)?.orgId,
           orgIdInDb: rows?.[0]?.org_id ?? null,
           usingTransaction: conn !== db,
         });
       } catch (err) {
         console.error("[tenant-context-diagnostic] failed:", err);
       }

     This logging block is for diagnostics during the smoke test. It will be removed in Step 5.

2.4  Show me the BEFORE and AFTER of the route's middleware chain plus the handler's first 15 lines.

2.5  Run npm run check 2>&1 | grep -c "error TS" — must match baseline.

STOP HERE. Show me Step 2's output. Wait