I need to understand how authentication and user→organisation mapping currently work in this codebase, before we modify the auth middleware. This is a READ-ONLY task. Do NOT modify, create, or delete any file. Do NOT restart the server. Do NOT run migrations.

Please gather and report the following:

SECTION 1 — Auth provider identification

1.1  List the contents of the server directory (top 2 levels):
     find server -maxdepth 2 -type f | sort

1.2  Check package.json for auth-related dependencies:
     cat package.json | grep -iE '"(passport|jsonwebtoken|jwt|@clerk|auth0|next-auth|express-session|cookie-session|@replit/repl-auth|lucia|better-auth|firebase-auth|supabase)"'

1.3  Search the codebase for which auth library is actually imported:
     grep -rn -E "from ['\"](passport|jsonwebtoken|@clerk|auth0|next-auth|express-session|@replit/repl-auth|lucia)" server/ shared/ --include="*.ts" --include="*.js" 2>/dev/null | head -50

SECTION 2 — Locate the auth middleware

2.1  Find files that look like auth middleware:
     grep -rln -E "(isAuthenticated|requireAuth|authenticate|verifyToken|passport\.(authenticate|session)|req\.user|req\.session\.user)" server/ --include="*.ts" --include="*.js" 2>/dev/null

2.2  Show the full contents of server/index.ts (or server/app.ts if index.ts doesn't exist):
     cat server/index.ts 2>/dev/null || cat server/app.ts 2>/dev/null

2.3  If a separate auth file exists (e.g. server/auth.ts, server/middleware/auth.ts, server/lib/auth.ts), show its full contents:
     for f in server/auth.ts server/middleware/auth.ts server/middleware/auth.js server/lib/auth.ts server/routes/auth.ts; do
       if [ -f "$f" ]; then echo "===== $f ====="; cat "$f"; fi
     done

SECTION 3 — User → organisation mapping (most important)

3.1  Show the users / organisations / membership table definitions from shared/schema.ts. Specifically extract:
     - the users table definition
     - the organisations table definition
     - any user_roles, user_organisations, memberships, organisation_members, or similar join table
     Use:
       grep -nE "(pgTable|export const).*\b(users|organisations|user_roles|user_organisations|memberships|organisation_members|members)\b" shared/schema.ts
     Then cat the relevant blocks. If you're unsure where each block ends, show 40 lines after each match:
       grep -nE "(pgTable|export const).*\b(users|organisations|user_roles|user_organisations|memberships|organisation_members|members)\b" shared/schema.ts | while read line; do
         lineno=$(echo "$line" | cut -d: -f1)
         end=$((lineno + 40))
         echo "===== lines $lineno-$end ====="
         sed -n "${lineno},${end}p" shared/schema.ts
       done

3.2  Check the actual database columns on the users table (ground truth, not just the schema file):
     SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name = 'users' ORDER BY ordinal_position;

3.3  Check whether user_roles has org_id (we backfilled it — confirm it's populated):
     SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name = 'user_roles' ORDER BY ordinal_position;
     SELECT COUNT(*) AS total_rows, COUNT(*) FILTER (WHERE org_id IS NOT NULL) AS with_org FROM user_roles;

3.4  Sample two rows from users (REDACT sensitive fields — show column names but mask email, password hashes, tokens with '***'):
     SELECT * FROM users LIMIT 2;
     (If you see email/password/token/secret columns in the output, replace their values with '***' before pasting back.)

3.5  Check whether users themselves carry an org_id column directly:
     SELECT column_name FROM information_schema.columns WHERE table_name = 'users' AND column_name IN ('org_id', 'organisation_id', 'organization_id');

SECTION 4 — How user identity is currently attached to requests

4.1  Find where req.user is set or session is established:
     grep -rn -E "(req\.user\s*=|req\.session\.user\s*=|passport\.serializeUser|passport\.deserializeUser|deserializeUser)" server/ --include="*.ts" --include="*.js" 2>/dev/null

4.2  Find a representative route handler that uses req.user, so we can see the current shape:
     grep -rn "req\.user\." server/ --include="*.ts" --include="*.js" 2>/dev/null | head -20

SECTION 5 — Existing tenant logic (if any)

5.1  Check whether any code already references orgId or org_id at runtime (not just in schema):
     grep -rn -E "(orgId|org_id|organisationId|organizationId)" server/ --include="*.ts" --include="*.js" 2>/dev/null | grep -v "schema" | head -30

5.2  Check whether any session/cookie setup references orgId:
     grep -rn -E "(orgId|activeOrg|currentOrg)" server/ shared/ --include="*.ts" --include="*.js" 2>/dev/null | head -20

PASTE BACK:

- All command outputs in order, labelled by section number
- File contents in full (do not truncate the auth files — I need to see them complete)
- The schema definitions for users, organisations, and any membership/role table
- Confirmation that you did NOT modify any file (a quick `git status` at the end would confirm this)

If any command produces no output, say "no output" — don't skip it. If a file doesn't exist, say so explicitly.

Strictly read-only. Do not propose changes or run anything else.