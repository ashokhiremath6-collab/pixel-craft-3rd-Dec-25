We are adding tenant-isolation middleware. This is a SMALL, ADDITIVE change. Do NOT modify localAuth.ts, replitAuth.ts, routes.ts, storage.ts, or any other existing file. Do NOT touch the running app's behaviour. Do NOT restart the server unless explicitly asked.

The goal: create a new file server/tenantAuth.ts that exports one new middleware (requireOrg) plus type augmentations, then wire only the TypeScript type declarations — no route changes yet.

STEP 1 — Pre-flight (read-only)

1.1  Confirm git is clean:
     git status

1.2  Locate the Express namespace declaration (if any) so we know whether augmentation already exists:
     grep -rn "declare global" server/ shared/ --include="*.ts" 2>/dev/null
     grep -rn "namespace Express" server/ shared/ --include="*.ts" 2>/dev/null

1.3  Check tsconfig for strictness — we want to know what compiler flags are active:
     cat tsconfig.json

1.4  Confirm server/tenantAuth.ts does NOT already exist (we want a clean create, not an overwrite):
     ls -la server/tenantAuth.ts 2>&1 || echo "does not exist (expected)"

STEP 2 — Create the new file at server/tenantAuth.ts with EXACTLY this content:

------------------- START FILE -------------------
// server/tenantAuth.ts
// Tenant isolation middleware.
//
// Why this file exists:
//   Authenticated requests already carry req.user (the full user row, hydrated
//   by passport.deserializeUser in localAuth.ts). That row includes org_id.
//   This file provides:
//     1. A typed shape for req.user so handlers stop using `(req.user as any)`.
//     2. A requireOrg middleware that enforces the user has an org_id and
//        exposes it as req.orgId for downstream handlers and storage calls.
//
// What this file deliberately does NOT do:
//   - It does not authenticate. Compose it AFTER isAuthenticated/requireAuth.
//   - It does not check roles. Compose it WITH requireAdmin/etc as needed.
//   - It does not modify req.user. It only reads orgId from it.

import type { Request, Response, NextFunction } from "express";

// ---- Type augmentation -----------------------------------------------------
// Express's default Request.user is `Express.User | undefined`. We narrow
// Express.User to the subset of fields the app actually relies on at runtime.
// This is a minimal contract: add fields here only when middleware/handlers
// genuinely need them at the type level. The runtime object still contains
// the full DB row — we are not stripping anything.

declare global {
  namespace Express {
    // eslint-disable-next-line @typescript-eslint/no-empty-interface
    interface User {
      id: string;
      orgId: string | null;
      isSuperAdmin?: boolean;
      email?: string;
    }

    interface Request {
      // Set by requireOrg. Non-null because requireOrg refuses to call next()
      // unless orgId is present.
      orgId?: string;
    }
  }
}

// ---- Middleware ------------------------------------------------------------

/**
 * Requires that req.user exists and has a non-null orgId.
 * Attaches req.orgId for convenience and type-narrowing in downstream code.
 *
 * Failure modes:
 *   - No req.user                       -> 401 { error: "not_authenticated" }
 *                                          (defensive; isAuthenticated should
 *                                          have caught this already.)
 *   - req.user.orgId is null/undefined  -> 403 { error: "no_organisation" }
 *
 * Compose AFTER an authentication middleware:
 *   app.get("/api/things", isAuthenticated, requireOrg, handler)
 */
export function requireOrg(req: Request, res: Response, next: NextFunction) {
  const user = req.user;
  if (!user) {
    return res.status(401).json({ error: "not_authenticated" });
  }
  if (!user.orgId) {
    return res.status(403).json({ error: "no_organisation" });
  }
  req.orgId = user.orgId;
  return next();
}

/**
 * Type guard / helper for inside handlers. Useful when a handler is composed
 * with requireOrg but TypeScript still treats req.orgId as optional.
 *
 *   const orgId = getOrgIdOrThrow(req);
 *   const rows = await storage.listThings(orgId);
 *
 * Throws (rather than returning a Response) so it surfaces as a 500 if
 * misused — i.e. if a route forgot to compose requireOrg. That noisy failure
 * is preferable to silently returning unfiltered data.
 */
export function getOrgIdOrThrow(req: Request): string {
  if (!req.orgId) {
    throw new Error(
      "getOrgIdOrThrow called without requireOrg in the middleware chain"
    );
  }
  return req.orgId;
}
------------------- END FILE -------------------

STEP 3 — Verify the file was written correctly:
  ls -la server/tenantAuth.ts
  wc -l server/tenantAuth.ts        (expect roughly 75-90 lines)
  head -5 server/tenantAuth.ts
  tail -5 server/tenantAuth.ts

STEP 4 — Type-check WITHOUT building or running:
  Run the project's typecheck command. Most likely one of:
    npx tsc --noEmit
  or
    npm run check
  or
    npm run typecheck

  If none of those exist, fall back to:
    npx tsc --noEmit -p tsconfig.json

  Report:
    - The exact command that worked
    - Full output (especially any errors)
    - Pay attention to errors mentioning: tenantAuth.ts, Express.User,
      Express.Request, declare global, namespace Express

  IMPORTANT: Some pre-existing TypeScript errors may already exist in the
  codebase (the original doc mentioned 5 mock-data errors missing orgId).
  Do not "fix" them. Just list every error and identify which ones are NEW
  (caused by tenantAuth.ts) vs PRE-EXISTING. Distinguish them clearly in the
  report.

STEP 5 — Confirm nothing else was modified:
  git status
  git diff --stat
  (Should show only server/tenantAuth.ts as new/untracked; no modified files.)

PASTE BACK:
  - Section 1 output (all four pre-flight checks)
  - Confirmation that the file was created with the correct line count
  - The exact typecheck command used and its FULL output
  - A clear breakdown: NEW errors vs PRE-EXISTING errors
  - git status + git diff --stat at the end

DO NOT:
  - Modify localAuth.ts, routes.ts, storage.ts, or any other existing file
  - Wire requireOrg into any route yet (that is a separate step)
  - Restart the server
  - "Fix" pre-existing TypeScript errors
  - Run npm install or modify package.json

If typecheck reports errors that are clearly caused by tenantAuth.ts (e.g. a
duplicate declare global for Express.User), STOP and report. Do not attempt
fixes. We will discuss the right resolution together.